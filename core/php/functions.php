<?php
    require_once 'ds.php';
    
    
    /**Implement later
    *
    *
    */
    function renderEmmet($text){
        $len = strlen($text);
        $stack = new SplStack();
        $item = "";
        for( $i = 0; $i < $len; $i++ ){
            $char = substr($text, $i, 1);
            
        } 
    }

    function render(){
        //
    }